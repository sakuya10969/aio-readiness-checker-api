version: "3.9"

services:
  aio-readiness-checker-api:
    container_name: aio-readiness-checker-api
    build:
      context: .
      dockerfile: Dockerfile
    image: ardev.azurecr.io/aio-readiness-checker-api:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    # command: >
    #   uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
